const state = {
    //导航栏
    isActive:'asdad',
}

 // getters
 const getters = {
    
}

// actions
const actions = {
 
}

const motations = {
    getActive(state, string){
        state.isActive = string
    }
}

export default{
    state,
    getters,
    actions,
    motations
}